# `nix-doom-emacs` is not great at assuming dependencies of doom modules :/

This dir is to cover for that.

Each developer ecosystem is not a complete kit, it's just the parts of the kit that doom relies on.
